apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-trader.fullname" . }}
  labels:
    {{- include "ai-trader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ai-trader.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
